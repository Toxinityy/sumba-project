# Backend review — 2026-09-21

Reviewed checkout `design/spec-and-prototype` at HEAD `e56321f`, with uncommitted frontend changes arriving during the review. This is a local code and test review, not a staging or production audit. Authority: `AGENTS.md`, the approved design spec, the foundations plan and their 2026-09-20 no-funding correction. Backend changes are in this checkout; no deployment or database migration was run.

## Implemented and reproduced

| Severity | Finding and evidence | Repair and regression check |
|---|---|---|
| Critical | `MediaAsset::isPublishable()` had no caller on publication or public query paths. A draft School with print-only child media could publish; a published School could accept that media; content stayed public when consent lapsed. Four new `SafeguardingTest` cases failed before the change. | `Publishable` now refuses publication with unsafe media and excludes it dynamically after consent lapses. `MediaAsset` refuses unsafe attachment to a published owner. All owning models use the same trait. The four cases pass, including an existing published School after consent expiry. |
| Critical | `Consent::coversWebUse()` ignored the stored child-assent flag and future grant dates. A consent record for an adult could also be applied to a child image. New model cases reproduced the first two failures. | The PHP check and database scope now require current web scope, effective dates, guardian and assent for a minor; child media requires a child consent record. New cases pass. |
| High | `Consent::withdraw()` saved withdrawal before unpublishing owners; a later write failure could leave a partial withdrawal. | The save and owner unpublishing now share a database transaction. An injected owner-save failure leaves the consent and School unchanged in a regression test. This covers database records; it does not purge files or external caches. |
| High | A record with no English slug rendered its Indonesian slug in an English directory, but route binding returned 404. A route test failed before the change. | `HasTranslations::whereSlug()` accepts the source slug only while the target slug is absent. Existing tests still reject the Indonesian slug when an English slug exists; School route and Post lookup fallback tests pass. |
| High | `DatabaseSeeder` could install review records plus a known-password `test@example.com` account in production. The production-mode test reproduced the account creation. | The bundled seeder now exits in production. The test verifies no demo user or School is inserted. Real content and staff provisioning remain deployment tasks. |
| Medium | The School editor accepted negative pupil counts, duplicate Indonesian slugs and a currency amount in `current_need`; Livewire regression tests failed before the change. | The form now requires nonnegative integral counts, per-locale unique slugs and rejects funding amounts in translated public copy. The tests pass. This is panel validation; direct database writes and effective-slug collisions across fallback locales still need a durable constraint or explicit validation before other write paths appear. |
| Medium | `deploy/cron.txt` claimed `--max-time=50` prevented overlapping minute workers. The installed Laravel worker checks max time after processing a job (`vendor/laravel/framework/src/Illuminate/Queue/Worker.php`, `stopIfNecessary`). | The cron command now uses `flock -n` and the same release path as `docs/deployment.md`. Confirm `/usr/bin/flock` and file-lock support on the real host before enabling it. |

`docs/admin-panel.md` was updated to reflect database-backed Posts and the production seeder guard.

Key code paths: `app/Models/Consent.php:56`, `app/Models/Concerns/Publishable.php:16`, `app/Models/MediaAsset.php:98`, `app/Models/Concerns/HasTranslations.php:53`, `app/Filament/Resources/Schools/Schemas/SchoolForm.php:27`, `database/seeders/DatabaseSeeder.php:13`, and `deploy/cron.txt:11`. The reproductions are in `tests/Feature/Models/{SafeguardingTest,SchoolRouteBindingTest,TranslationTest,DatabaseSeederTest}.php` and `tests/Feature/Admin/SchoolPanelTest.php`.

## Remaining findings, ordered for repair

| Priority | Finding and evidence | Smallest next step / owner / regression check |
|---|---|---|
| P0 — launch blocker | **Media remains a placeholder flow.** `app/Models/MediaAsset.php:98` returns `PlaceholderImage`; `app/Services/Images/VariantGenerator.php:98` writes private `storage/app/variants` files that no public image array references. There is no panel upload path. A child image copied into the public disk before `MediaAsset::saving` runs could be fetched directly even if consent blocks the record. `public/storage` is absent in this checkout. Code inspection; real upload exposure is not yet executable because upload UI is missing. | Backend/media owner: design the upload boundary on private storage, strip metadata before any public copy, validate content/dimensions, generate the named crops and bounded variants, then expose only approved files. Verify revocation by fetching a formerly public URL, not just querying the database. Confirm whether the planned media-library dependency is needed before adding it. |
| P0 — launch blocker | **Child names can be written outside model events.** `database/migrations/2026_09_17_000002_create_content_tables.php:96` and `2026_09_17_000001_create_safeguarding_tables.php:65` retain surname columns. `Post::saving` and `MediaAsset::saving` reject surnames on minor records through Eloquent, but bulk/raw updates bypass those hooks. This conflicts with spec §9's structural no-surname requirement. | Backend/data owner: choose a schema-level constraint or separate adult identity storage compatible with the host's MySQL version, migrate existing data safely, and test a raw update. Have the child-protection specialist review the actual policy. |
| P1 | **Empty homepage returns 500.** A temporary Pest probe with `RefreshDatabase` and no seed called `GET /id` and got `Trying to access array offset on null` at `routes/web.php:64` (`PostData::find('ibu-maria-bulu')['quote']`). The probe was removed so the suite remains runnable. Editing or withdrawing the named post has the same risk; the featured School is also hard-coded. | Frontend/pages owner: render a valid empty state or omit the relevant section when no published featured content exists. Keep a request test for an empty database and for a withdrawn or renamed featured record. Do not seed fabricated content to mask the failure. |
| P1 | **Panel workflow is incomplete.** Only Schools have a Filament resource; Posts, media/consent review, draft preview, revisions and undo are absent. `docs/admin-panel.md` and Filament resource inventory confirm this. It misses phase 2's Vera school-and-post end-to-end acceptance criterion. | Backend/editor owner: build the Post edit/publish path and consent-aware upload first; then signed draft preview and revision restore. Test the actual create → preview → publish → revert workflow in both locales. |
| P1 | **Withdrawal reaches attached owners, not rich-text images or direct file URLs.** `Consent::withdraw()` walks `mediaAssets()->attachable`; the rich-text image rewrite pass is not present. Generated and cached media are unaffected. | Backend/media owner: make body images first-class tracked assets before enabling rich text uploads, then verify withdrawal removes every public reference and URL or blocks access immediately. |
| P1 | **Missing translation note.** `app/Models/Concerns/HasTranslations.php:28` can identify fallback to Indonesian, but no view calls it (`rg translationLocale resources/views` found none). The rendered English page therefore gives no quiet fallback notice required by spec §7. | Frontend/pages owner: surface the locale metadata in the existing view-model contract by agreement with both workstreams, then assert the English page shows the translated notice only when it falls back. |
| P1 | **No staging restore or host capability evidence.** `docs/deployment.md` says no host has been selected. The local `images:capabilities` reports GD with AVIF/WebP/JPEG; Imagick and the host remain unverified. The deployment path assumes a writable release, PHP 8.3+, `intl`, Cloudflare origin restriction and cron access. | Deployment owner: provision same-host staging, test production dependency/Filament asset upload, `storage:link` or equivalent media delivery, mail, a database-plus-media restore, and origin restrictions. Record dated results in `docs/deployment.md`. |
| P2 | **Seed reruns and effective slug uniqueness.** `SchoolSeeder` uses `School::create` for each fixed record, so rerunning it duplicates content. School panel uniqueness checks exact per-locale JSON paths; a target-locale slug can still collide with another record's fallback source slug. | Backend/data owner: if seed reruns will be part of deployment, make them idempotent without overwriting editorial changes; otherwise document one-time import. Add a collision test before opening more write paths. |
| P2 | **Production mail and proxy need host proof.** The current frontend changes passed targeted contact tests. `bootstrap/app.php` trusts all proxies and the deploy runbook requires the origin to accept only Cloudflare; no host exists to verify that boundary. `AppServiceProvider` rejects `log` mail but cannot prove SMTP credentials deliver. | Deployment owner: on staging, try a real received enquiry, direct-origin request with forged forwarded IP and throttling; confirm DNS/SPF and the operator's failure alert. Keep the frontend's browser and JSON recovery tests. |
| P2 | **No CI evidence for the safeguarding suite.** No `.github` directory or other tracked CI workflow was found in this checkout. | Repository owner: attach the existing `php artisan test` command to the chosen CI provider before launch; verify a failing safeguarding test blocks the build. |

## Validation and limits

- Baseline before edits: model/admin 70 tests, 197 assertions passed; image/mail group 23 tests, 37 assertions passed.
- Focused post-change safeguarding, locale and School panel tests passed; frontend contact/production/mail integration group passed 20 tests, 183 assertions.
- Local runtime: PHP 8.3.10 with GD, EXIF, `intl` and SQLite; no Imagick. SQLite tests do not prove MySQL JSON behavior or host filesystem permissions.
- The existing EXIF test covers a geotagged JPEG through the model save path. It does not cover unregistered public-disk files, Imagick, all variants or cached URLs.
- Final integrated run: `php artisan test --compact` passed 336 tests and 1,334 assertions. Pint passed on the changed backend PHP files; `git diff --check` passed. Concurrent frontend edits mean this review is not a frozen-release audit.
